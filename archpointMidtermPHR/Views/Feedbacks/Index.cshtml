@model IEnumerable<archpointMidtermPHR.Models.Feedback>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HowArchpointWasFound)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EasyToUnderstand)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SuggestionsForImprovements)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HowArchpointWasFound)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EasyToUnderstand)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SuggestionsForImprovements)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FeedbackId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FeedbackId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FeedbackId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
