@model IEnumerable<archpointMidtermPHR.Models.DoctorVisitForm>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CurrentComplaint)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CurrentMedQuestion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CurrentComplaint)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CurrentMedQuestion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ReasonForVisitId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ReasonForVisitId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ReasonForVisitId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
